# Examples

Run the minimal example directly:

```bash
npm run build
node dist/src/cli.js inspect examples/basic-fixture.json --output .tmp/example
```

The generated report is written to `.tmp/example/opikprobe-report.md`.
